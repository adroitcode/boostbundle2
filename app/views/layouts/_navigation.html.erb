<a href="/#info" class="links" >LEARN MORE</a>  

<% if not user_signed_in? %>
  <%= link_to 'SIGN UP', new_user_registration_path(:plan => 'silver'), :class => 'links', :id => 'sign_up' %>       
<% end %>


<% if user_signed_in? %>
  <%= link_to 'EDIT ACCOUNT', edit_user_registration_path, :class => 'links' %>
  <% if current_user.has_role? :admin %>
    <%= link_to 'ADMIN', users_path, :class => 'links' %>
  <% end %>
<% end %>


<% if user_signed_in? %>
  <%= link_to 'LOGOUT', destroy_user_session_path, :method=>'delete', :class => 'links' %>        
<% else %>
  <%= link_to 'LOGIN', new_user_session_path, :class => 'links' %>  
<% end %>



